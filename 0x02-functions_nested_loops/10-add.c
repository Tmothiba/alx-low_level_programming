#include "main.h"
#include <stdio.h>
/**
 * add- adding two numbers
 * @a: number to be added
 * @b: number to be added
 * Return: added output
 */
int add(int a, int b)
{
	int added;

	added = a + b;
	return (added);
}
