#include "main.h"
#include <stdio.h>
/**
 * mul - multiplication of two numbers
 * @a: number to be multiplied
 * @b: number to be multiplied
 *
 * Return: 0 (Success)
 */
int mul(int a, int b)
{
	int sum;

	sum = a * b;
	return (sum);
}
