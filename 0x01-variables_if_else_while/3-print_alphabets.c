#include <stdio.h>
/**
 * main - printing last didit of a number
 *
 * Return: Always 0 (Succes)
 */
int main()
{	
	int alphabet = 97;
	int alphabet2 = 65;
	while( alphabet <= 122)
	{
		putchar(alphabet);
	}	
	 while (alphabet2 <= 90)
	 {
		 putchar(alphabet2);
	 }

	putchar('\n');
	return(0);
}
