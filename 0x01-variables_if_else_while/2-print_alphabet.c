#include <stdio.h>
/**
 * main - printing last didit of a number
 *
 * Return: Always 0 (Succes)
 */
int main()
{
        char albet = 'a';
        for( albet = 'a'; albet <= 'z';albet++)
        {
                putchar(albet);
        }
        putchar('\n');
        return(0);
}
